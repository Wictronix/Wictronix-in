import { Metadata } from "next";
import ClientPage from "./ClientPage";

export const metadata: Metadata = {
  title: "Contact WictroniX | Cross-Functional Growth for Business",
  description: "Partner with WictroniX for B2B Marketing, SEO/GEO, and Website Development. Close the gap between strategy and execution today.",
};

export default function Page() {
  return <ClientPage />;
}
